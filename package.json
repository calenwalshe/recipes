{
  "name": "recipes",
  "version": "1.0.0",
  "description": "Responsive recipe explorer UI",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "vitest": "^1.6.0"
  }
}
